//! Python-facing dataclass equivalents (Pydantic model mirrors).
//! Currently a placeholder — no shared types needed yet.
//! When we add trackers with shared config types (Charuco, RTMPose),
//! their Python-facing structs will live here.
