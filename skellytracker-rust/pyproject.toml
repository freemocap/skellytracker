[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "skellytracker-rust"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "_skellytracker_rust"
